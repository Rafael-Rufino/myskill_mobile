import { StyleSheet } from 'react-native'
export const styles = StyleSheet.create({
  container: {
    flexDirection: 'column',
  },
  text: {
    color: 'orange',
    fontSize: 24,
    fontWeight: 'bold',
  },
  textHour: {
    color: '#f1f1f1',
    fontSize: 16,
    opacity: 0.8,
  },
})
